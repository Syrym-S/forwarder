import ReactDOM from "react-dom/client";
import SharedLeadApp from "./shared-lead/shared-lead-app";
import "../../src/leaflet-icon";

ReactDOM.createRoot(document.getElementById("root")).render(<SharedLeadApp />);
