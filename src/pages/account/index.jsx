import { Alert, AlertTitle, Typography } from "@mui/material";
import React from "react";
import RootLayout from "../../components/layout/root-layout";

const Account = () => {
  return <RootLayout>Account</RootLayout>;
};

export default Account;
